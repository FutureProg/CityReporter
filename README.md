# City Reporter

The "City Reporter" app will provide users the ability to take photos and email their city via the Gmail API. 
- Gmail was chosen to avoid server costs, and keep the app front-end focused
    - need to confirm that Gmail's OAuth doesn't require server-side work in order to make use of the API
- The prototype will be a web application and expanded to a native mobile app